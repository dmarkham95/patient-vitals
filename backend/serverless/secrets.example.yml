##
## This is an example yml. Fill the secrets and rename the file as 'secrets.yml'
##

default: &default
  <<: *default
  DB_NAME: NestJs # For TypeOrm DB Setup
  DB_USER: nestjs_db_admin
  DB_PASSWORD: password
  DB_HOST: local
  DB_PORT: 3306

Dev:
  <<: *default
  DB_NAME: NestJs # For TypeOrm DB Setup
  DB_USER: nestjs_db_admin
  DB_PASSWORD: password
  DB_HOST: local
  DB_PORT: 3306

Prod:
  <<: *default
  DB_NAME: NestJs # For TypeOrm DB Setup
  DB_USER: nestjs_db_admin
  DB_PASSWORD: password
  DB_HOST: local
  DB_PORT: 3306
